# Add your frontend URL here (comma-separated if multiple)
# For local development, this can be empty as local origins are already allowed
# For production on Render, add your deployed frontend URL
ALLOWED_ORIGINS=https://code-cast1.vercel.app

# JDoodle API credentials (required for code compilation)
JDOODLE_CLIENT_ID=
JDOODLE_CLIENT_SECRET=

# Port (Render will set this automatically)
PORT=5000
